#include "debug/Debug.h"

// Este archivo de implementación ha sido combinado con Debug.cpp
// para evitar errores de enlazado debido a definiciones duplicadas.
// Las funciones Init(), Debug(), Warning(), Error() y Shutdown()
// ahora están implementadas sólo en Debug.cpp.

// Si se necesita una implementación diferente en el futuro,
// se puede considerar usar `inline` o declaraciones `extern`
// para evitar conflictos de enlazado.